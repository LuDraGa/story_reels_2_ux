# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key (SERVER-SIDE ONLY - do NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Modal Coqui TTS API (External - Already Exists)
COQUI_API_BASE_URL=https://abhirooprasad--coqui-apis-fastapi-app.modal.run

# ==========================================
# GitHub Actions Secrets (for Supabase Heartbeat)
# ==========================================
# These need to be added as Repository Secrets in GitHub:
# Settings → Secrets and variables → Actions → New repository secret
#
# Required secrets:
# - SUPABASE_URL: same as NEXT_PUBLIC_SUPABASE_URL above
# - SUPABASE_ANON_KEY: same as NEXT_PUBLIC_SUPABASE_ANON_KEY above
#
# See docs/HEARTBEAT.md for setup instructions
